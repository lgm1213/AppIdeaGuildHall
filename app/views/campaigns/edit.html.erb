<%= content_for :title, "Edit #{@campaign.name} — The Chronicler" %>

<div class="max-w-2xl mx-auto">
  <div class="mb-8">
    <h1 class="font-headline text-4xl font-bold text-primary tracking-tight mb-2">Edit Campaign</h1>
    <p class="text-on-surface-variant"><%= @campaign.name %></p>
  </div>

  <%= render "form", campaign: @campaign, game_systems: @game_systems %>
</div>

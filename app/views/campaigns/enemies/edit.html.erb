<%= content_for :title, "Edit #{@enemy.name} — The Chronicler" %>

<div class="max-w-2xl mx-auto">
  <div class="mb-8">
    <p class="text-xs text-primary font-bold uppercase tracking-widest mb-1"><%= @campaign.name %></p>
    <h1 class="font-headline text-4xl font-bold text-on-surface tracking-tight">Edit Enemy</h1>
    <p class="text-on-surface-variant"><%= @enemy.name %></p>
  </div>

  <%= render "form", enemy: @enemy, campaign: @campaign %>
</div>

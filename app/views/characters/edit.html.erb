<%= content_for :title, "Edit #{@character.name} — The Chronicler" %>

<div class="max-w-2xl mx-auto">
  <div class="mb-8">
    <h1 class="font-headline text-4xl font-bold text-on-surface tracking-tight mb-2">Edit Character</h1>
    <p class="text-on-surface-variant"><%= @character.name %></p>
  </div>

  <%= render "form", character: @character %>
</div>
